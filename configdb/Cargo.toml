[package]
name = "sable-config-db"
version = "0.1.0"
edition = "2021"

[dependencies]
libsabledb = { path = "../libsabledb" }
clap = { version = "4", features = ["derive"] }
tracing = { version = "0", features = ["log"] }
tracing-subscriber = "0"
tracing-appender = "0"
ctrlc = "3.4.0"
